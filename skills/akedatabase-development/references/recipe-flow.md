# 配方查看器

- 输入与输出方向保持正确，保留分支，不把合成链压成单一路径。
- 滚轮以光标为中心缩放；固定画布高度，通过拖动查看全景。
- 展开只向下延伸，保持上方布局和当前全屏元素，不重建导致退出全屏的容器。
- 搜索仅包含有配方的物品；装备结果按现有“装备制造”汇总约定处理。
- 导出当前显示的完整配方链，不仅导出视口。导出副本必须有明确不透明背景及可被 html2canvas 解析的颜色。
- 环境与配方数据共用公共解析，环境名称或颜色缺乏表字段时记录例外，不能凭特效文件名认定最终视觉颜色。
